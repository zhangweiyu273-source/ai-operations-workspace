import { fireEvent, render, screen, waitFor } from "@testing-library/react";
import { beforeEach, describe, expect, it, vi } from "vitest";
import AISettingsPage from "./page";
const api = vi.hoisted(() => ({ get: vi.fn(), post: vi.fn() })); vi.mock("@/lib/api/client", () => ({ apiClient: api }));
describe("AISettingsPage", () => { beforeEach(() => { vi.clearAllMocks(); api.get.mockImplementation((path: string) => Promise.resolve(path.includes("status") ? { configured: true, provider: "deepseek", model: "deepseek-chat", provider_status: "configured" } : { today_calls: 1, success_count: 1, failure_count: 0, total_tokens: 3, average_latency_ms: 120 })); api.post.mockResolvedValue({ provider: "deepseek", model: "deepseek-chat", content: "AI运营工作台连接测试成功。", usage: { total_tokens: 3 }, latency_ms: 120 }); }); it("loads safe status and runs a provider test", async () => { render(<AISettingsPage />); await screen.findByText("已配置"); expect(screen.queryByText("test-key")).not.toBeInTheDocument(); fireEvent.click(screen.getByRole("button", { name: "测试 AI Provider" })); await waitFor(() => expect(api.post).toHaveBeenCalledWith("/ai/test", { message: "请回复：AI运营工作台连接测试成功。" })); await screen.findByText("AI运营工作台连接测试成功。"); }); });
