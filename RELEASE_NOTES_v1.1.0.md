# AI运营工作台 v1.1.0 — Knowledge Search

发布日期：2026-08-22
发布标签：`v1.1.0-knowledge-search`

## 1. 新增功能

- 知识库统一检索覆盖标题、摘要、正文、标签和分类。
- 搜索结果展示命中的 Markdown 词条及其解释，而非只展示文档标题。
- 知识文档标题可进入只读详情页，集中查看分类、标签、全部词条、词条解释与完整正文。
- 支持通过搜索按钮和 Enter 键提交同一搜索操作。

## 2. 修复问题

- 修复搜索只匹配标题、正文和摘要，遗漏标签与分类的问题。
- 修复知识库搜索操作缺少明确提交入口、Enter 不触发查询的问题。
- 修复本地 Docker 前端在源码更新后继续提供旧版页面的验证缺口；本次发布前已重建并验证前端生产镜像。

## 3. 测试结果

- 后端知识库 API：2/2 通过，覆盖标签、分类和正文的独立命中场景。
- 后端 Ruff：通过。
- 前端 Vitest：3 个测试文件、8/8 通过，覆盖 XSC、MK、HD 词条、搜索按钮、Enter 搜索和详情页展示。
- 前端 ESLint：通过。
- 前端 production build：通过，包含 `/knowledge/[id]` 路由。
- Railway 线上只读验证：`search=XSC` 与 `search=HD` 均返回 HTTP 200、1 条词典文档及对应解释来源。

## 4. 部署状态

- `ai-ops-api`：SUCCESS，部署 `08817d16-2f5c-4ded-bcd6-8c60556c84c7`。
- `ai-ops-workbench`：SUCCESS，部署 `9e0c47fd-f8a3-467a-937e-b5bde12608ae`。
- 数据库结构未变化；未执行 Migration，未修改环境变量，未重新导入知识数据。
- 线上入口：[AI运营工作台](https://ai-ops-workbench-production-57b4.up.railway.app)。

## 5. 已知限制

- 词条级展示以 Markdown `###` 标题作为结构化词条边界；无该结构的旧知识文档仍可搜索，但详情页会以“文档正文”摘要展示。
- 当前搜索使用数据库包含匹配；知识规模显著增长后，可在独立性能评估后考虑 PostgreSQL 全文检索，但不属于本次版本范围。
- 当前线上访问仍受 HTTP Basic Auth 保护；未认证请求返回 401 属于预期行为。
