# AI运营工作台开发规则

本文件对仓库内所有目录生效。目标是保持系统长期可维护、可迁移、可测试。

## 任务开始前

1. 完整阅读 `AGENTS.md`。
2. 阅读 `ARCHITECTURE.md`。
3. 阅读 `BUG_HISTORY.md`。
4. 明确当前任务的目标、范围、数据库影响、验收标准和测试要求。
5. 执行 `git status --short --branch`，识别并保护已有修改。

## 开发约束

- 不随意重构无关模块，不修改无关文件，不删除已有正常功能。
- 不私自更换 Next.js、FastAPI、PostgreSQL、SQLAlchemy、Alembic 技术栈。
- 不通过前端写死数据、固定返回值或删除异常逻辑掩盖问题。
- 配置、密钥、地址和路径必须来自环境变量或统一配置；严禁提交真实密钥。
- 核心业务数据写入 PostgreSQL；表结构变化必须通过 Alembic migration。
- 应用启动不得使用 `metadata.create_all()` 代替 Migration；测试内存数据库除外。
- Migration 必须审查 upgrade/downgrade，并在独立测试数据库完成往返验证后再用于正式数据。
- 核心表需预留 `organization_id`、`created_by`、`updated_by`，并按数据风险采用软删除。
- 上传文件统一进入配置的存储目录，不散落在源码目录。
- API 路由保持版本化；业务层不得直接绑定具体 AI 厂商。
- AI 不可用时，非 AI 模块必须继续工作并返回可诊断错误。
- 新依赖必须说明用途，检查 License、维护状态和体积，避免架构绑死。
- 错误应携带可追踪上下文，但日志不得泄露密码、令牌或敏感用户数据。
- API 错误统一返回错误码、用户安全消息与 request ID；500 异常记录服务端日志但不向客户端暴露堆栈。

## Task 标准

每个 Task 在实施前应明确：任务目标、涉及模块、修改范围、数据库影响、验收标准、测试要求。一次只处理一个或一组强相关 Task。

## Bug 标准流程

复现 → 定位 → 确认根因 → 检查历史问题 → 修复根因 → 搜索同类问题 → 增加必要测试 → 运行相关测试 → 更新 `BUG_HISTORY.md` → 回归验证。

禁止无限 `try/except`、隐藏异常、跳过验证或用写死返回值修复 Bug。

## 完成标准

- 运行与变更风险相匹配的单元、API、数据库和前端测试。
- 按 `CODEX_CHECKLIST.md` 检查并如实记录无法执行的项目。
- 重大架构调整同步更新 `ARCHITECTURE.md`；用户可见变更更新 `CHANGELOG.md`。
- 重要 Bug 修复按模板更新 `BUG_HISTORY.md`，可复发问题增加回归测试。
- 核心检查失败时不得宣布完成。
- 完整 Task 测试通过后再提交，提交信息应准确描述单一变更范围。

## 重复问题强制升级机制

满足以下任一条件即为 **Recurring Issue / 重复问题**：已修复 Bug 再次出现、同一验收项下一轮仍未完成、已宣布通过但独立验收失败、BUG_HISTORY 中问题复发，或同一种修复无效。

- 第二次出现必须标记 `RECURRING ISSUE`，暂停相关新功能，禁止重复第一次方案。
- 必须以现象、运行环境、日志、代码、配置、数据库、Docker、测试和验收标准重新定位，并以证据解释第一次未关闭：根因误判、表面修复、未进入运行环境、未真实验证、测试不足、环境/Migration/缓存、验收误解、方案不可靠、工具限制或错误宣布完成。
- 必须提出机制化防复发措施（回归测试、启动/Docker/Migration/配置检查、日志、流程、验收或脚本）；可自动化问题必须新增回归测试。
- 验收项重复未完成属于“重复验收失败”。完成状态严格区分“已实现/已执行/已验证/已验收”；只有真实验证满足要求才可宣布完成。
- 第三次出现升级为 `PERSISTENT ISSUE`：停止普通修补，输出时间线、前两次方案及失败原因、当前根因、方案 A/B、推荐方案、影响和风险；不得再用原方法。
- 重复问题报告必须包含首次方式、首次未解原因、本次根因和修复、方案差异、防重复优化、新增测试、真实验证及关闭状态（仅：已关闭/仍需验证/未关闭）。重复故障优先使用 5.6 Sol。
